apiVersion: v1
kind: Service
metadata:
  name: banking-app
  labels:
    app: banking-app
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: banking-app
